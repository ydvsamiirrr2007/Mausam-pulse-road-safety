[project]
name = "mausam-pulse-road-safety"
version = "0.2.0"
description = "Real-time road-weather safety dashboard with live mapping and braking analysis"
requires-python = ">=3.11"

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
